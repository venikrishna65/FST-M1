package testNGTests;

import org.testng.annotations.Test;

public class Activity8 {
  @Test
  public void f() {
	  //Create the testng.xml file and add all the activities to the list of classes to execute.
  }
}
