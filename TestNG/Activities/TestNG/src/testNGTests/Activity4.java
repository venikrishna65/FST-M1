package testNGTests;

import org.testng.annotations.Test;

public class Activity4 {
  @Test
  public void f() {
	  //Create the testng.xml file and add both the classes Activity1 and Activity2 to the list of classes to execute.
  }
}
